{
	"name": "ws-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"devDependencies": {
		"@types/node": "^25.5.0",
		"typescript": "^5.5.2",
		"vitest": "^4.1.0",
		"wrangler": "^4.73.0"
	},
	"dependencies": {
		"aws4fetch": "^1.0.20"
	}
}
